name: run-python-application

on: 
  workflow_dispatch:
    inputs:
      name:
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v6
      with:
        python-version: "3.10"
    
    - name: Run python file # the context is placed inside quotes to prevent splitting when passed in through the command line
      env:
        name: ${{ inputs.name }}
      run: |
        python3 hello.py "$name"
